# `docs/development/research/native-antigravity.md`

- 형식: `100644`
- 바이트: 3205
- SHA-256: `90f7191a8995ccbbceccb465ee7e436a47e858975a698805334804d286ef4eec`
- 인코딩: `utf-8`

```
# Antigravity 실호스트 발견 계약 및 실증 관찰

확인일: 2026-09-08. 앱 버전: Antigravity 2.11.0. 프로젝트 디렉터리에 플러그인을 임시 설치한 후 호스트 환경 설정 화면의 인식 상태를 직접 확인했습니다. 불필요한 모델 API 프롬프트는 발행하지 않았습니다.

## 관찰 결과

1. 플러그인 설치 전 프로젝트 환경의 스킬 수는 16개, 메타데이터 컨텍스트는 1,677 토큰으로 계측되었습니다.
2. 플러그인 설치 후 스킬 수는 19개, 메타데이터 컨텍스트는 2,005 토큰으로 증가했습니다. 세부 목록에 `gtg-build`(125 토큰), `gtg-interface`(116 토큰), `gtg-research`(87 토큰)가 정상 인식되었습니다.
3. 초기에 생성했던 `rules/gtg.md` 파일은 호스트의 규칙 목록에 포함되지 않았습니다. 당시 규칙 수는 기존 2개(3,636 토큰)로 변동이 없었습니다.
4. 앱에 내장된 공식 문서를 검토하여 `rules/AGENTS.md`가 단일 표준 진입점임을 확인했습니다. 이 규격에 맞추어 패키지 구조를 변경한 뒤 설정을 재조회하자 규칙 수가 3개(4,208 토큰)로 정상 갱신되었습니다.
5. 검증 완료 후 프로젝트별 설치본을 제거 명령을 통해 백업 디렉터리로 안전하게 이동시켰습니다. 전역 프로필이나 기본 모델, 권한 설정은 일체 변경하지 않았습니다.

위 시험은 호스트 파일 시스템 수준의 정적 리소스 발견과 컨텍스트 바인딩에 대한 실증 근거입니다. 모델이 실제 추론 과정에서 스킬을 적절히 선택하거나 훅이 의도대로 발행되었는지까지 포괄하지는 않습니다. 표기된 토큰 수치는 UI 설정 화면의 컨텍스트 점유량이며 실제 API 과금량이 아닙니다.

## 호스트 내장 공식 문서 대조

기준 디렉터리는 `~/.gemini/antigravity/builtin/skills/agy-customizations/`입니다. 해당 문서는 읽기 전용으로 대조했으며, 아래 해시값은 조회 당시의 원본 파일 기준입니다.

| 문서 | SHA-256 | 확인한 인터페이스 계약 |
| --- | --- | --- |
| `SKILL.md` | `79c8885e76ce449e82016b37b53dfcc3ff5afefd90a23ea83b24148f41f33fa1` | 플러그인 탐색 위치, 우선순위 해석, 온디맨드 점진적 로딩 규칙 |
| `docs/rules.md` | `969a834145a09398b53cd63b7d473d8a915bf80928726650a6d2efc602e33bbe` | AGENTS.md 및 GEMINI.md의 디렉터리 스코프 적용 방식 |
| `docs/plugins.md` | `481c2612f4729fb4fab600aebc5eb0d4eda6e00d5b45084bc7523718514bf19e` | 플러그인 내 규칙 배치는 rules/AGENTS.md 형식을 권장 |
| `docs/hooks.md` | `6c76fe162f89527b7494213f11fdb268eb508bfca22a7af61f86ced285340bf4` | camelCase 네이밍 규약, 훅 파일 위치를 작업 디렉터리로 삼는 실행 규칙 |

공개 웹 문서와 실제 설치된 호스트 앱의 내장 문서 간에 세부 설명의 차이가 존재했습니다. [공개 규칙 문서](https://antigravity.google/docs/rules-workflows)와 [플러그인 문서](https://antigravity.google/docs/plugins)를 기본 참조로 삼되, 최종 파일 배치는 위 실호스트 발견 시험을 통해 확정했습니다.

```
