# `docs/development/validation/2026-09-08-package/README.md`

- 형식: `100644`
- 바이트: 3144
- SHA-256: `8e2f4fd37da4d0e0f619ec59ed87a1256227d936f806b9f648b395d328582b65`
- 인코딩: `utf-8`

```
# 패키지 원문과 훅 검증

전체 로컬 검사 111개가 28.270초에 통과했습니다. [실행 로그](tests.log)와 [당시 소스 해시·명령·종료 코드](verification.json)를 보존했습니다. 추가 모델 요청은 0회입니다.

## 재현한 실패와 수정

| 실패 | 수정 | 직접 확인 |
| --- | --- | --- |
| Gemini CLI 규칙의 Markdown 구분선 앞 내용이 사라지고 CRLF가 바뀜 | 규칙을 바이트 그대로 복사 | 구분선 앞뒤와 CRLF를 포함한 원문 전체 일치 |
| 필수 원본 파일·폴더의 링크를 따라가거나 민감 파일을 패키지에 복사 | 원본 경로 검사와 일반 파일만 여는 읽기 경로 적용 | VERSION·진입점·규칙·스킬 폴더 링크와 합성 `.env` 거부 |
| 완료 훅 대신 컨텍스트 훅을 두 번 넣어도 개수 검사 통과 | 두 필수 이벤트와 명령 인자를 각각 검사 | 중복·누락·다른 이벤트 인자 거부 |
| Gemini CLI 훅이 선언한 1밀리초를 무시하고 고정 5초로 검사 | 선언된 호스트별 단위를 적용하고 검사 상한 5초 유지 | 지연된 실제 하위 프로세스가 시간 초과로 실패 |
| 비활성 필수 훅에 TypeError가 발생 | 구조·활성 여부를 실행 전 검사 | 비활성·잘못된 그룹·필터·유효하지 않은 시간 제한 거부 |
| 프로젝트를 옮겨 실제 훅 경로가 깨져도 doctor가 성공 | 설치 전 staging의 목적 경로와 활성 설치의 실제 경로를 구분 | 이동 후 doctor 실패, 새 경로 재설치 후 정상 검사 |

원본 관련 신규 검사 4개는 수정 전 모두 실패했습니다. 훅 관련 초기 검사 3개는 수정 전 실패 2개·오류 1개였습니다. 추가 이동 경로 수정까지 마친 뒤 관련 검사 10개와 설치 검사 12개가 통과했고, 이어 전체 검사를 실행했습니다.

검증기는 GTG가 생성하는 두 필수 훅의 계약을 검사합니다. 호스트의 모든 가능한 사용자 훅을 해석하는 범용 검증기가 아닙니다. 정적 검사도 이벤트·인자·구조를 확인하지만 실제 실행 시간은 훅 실행 검사에서만 확인합니다.

폴더 이동 검사도 수정 전 `ok: true`·종료 코드 0으로 잘못 통과했습니다. 경로 검사를 추가한 뒤 이동 경고와 재설치 복구가 통과했습니다.

한글·공백·따옴표·명령 치환 문자가 포함된 임시 경로에서 생성한 훅을 `/bin/sh`로 직접 실행했습니다. 패키지 밖의 작업 디렉터리에서 양쪽 호스트의 이벤트 네 개가 예상 JSON을 반환했고, 경로 안의 표식 명령은 실행되지 않았습니다. [실행 결과](shell-contract.json). 이는 셸 명령 계약 검사이며 실제 호스트 이벤트 발행을 대신하지 않습니다.

훅 명령은 설치 위치에 맞춘 절대 경로입니다. 프로젝트를 옮긴 뒤에는 새 위치에 재설치해야 합니다. Gemini CLI의 `${extensionPath}` 치환은 공식 문서에서 확인했으나, 이동 시 변수 치환·셸 인용 계약을 검증하지 않은 채 적용하지 않았습니다.
```
