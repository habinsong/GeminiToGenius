# `docs/rebuild/validation/2026-09-08-inspection/README.md`

- 형식: `100644`
- 바이트: 1610
- SHA-256: `6e8d9779eaff60f359b444572c983f043732ef7638d24a081795eacd5ba50101`
- 인코딩: `utf-8`

```
# 코드 조회 검증

추가 모델 호출 없이 수행했습니다. 이전 실제 사례의 초기 소스 파일 5개를 설치된 `inspect` 명령으로 한 번에 읽고 원문 바이트와 대조했습니다. [측정 결과](batch-read.json).

- `records.py`, `test_records.py`, `app.py`, `notes.txt`, `README.md`의 전체 내용이 반환됐습니다.
- 본문은 1,125바이트, JSON 전체는 3,460바이트였습니다. 내용 예산과 실제 직렬화 크기를 구분합니다.
- 모든 내용과 파일 해시가 원본과 일치했습니다. CLI 한 번의 실행 시간은 약 0.176초였습니다.

이전 모델 기록의 첫 소스 읽기는 5번의 개별 호출이었습니다. 이것은 묶을 수 있는 작업의 존재를 확인한 것이며, 새 명령을 실제 모델이 선택했거나 전체 호출 수·시간·품질이 개선됐다는 결과는 아닙니다. 네이티브 다중 파일 읽기 도구가 있는 환경에서는 해당 도구를 우선합니다.

회귀 검사에는 줄 범위 완료와 파일 전체 완료의 구분, 예산 소진 후 나머지 파일의 명시, UTF-8 경계, 긴 한 줄, 바이너리·다른 인코딩, 스캔 한도, 파일·부모 디렉터리 교체, 심볼릭 링크·특수 파일·민감 경로를 포함합니다.

채점기는 제품 코드를 import하지 않도록 분리했습니다. 제품 import를 차단한 프로세스에서 채점이 동작하는 것을 확인했으며, 숫자 0을 불리언 false로 바꾼 잘못된 결과도 실패 처리합니다. 같은 수를 나타내는 정수·실수 차이는 허용합니다.
```
