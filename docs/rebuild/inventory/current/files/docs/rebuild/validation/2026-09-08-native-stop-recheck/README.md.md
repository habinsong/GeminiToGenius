# `docs/rebuild/validation/2026-09-08-native-stop-recheck/README.md`

- 형식: `100644`
- 바이트: 3972
- SHA-256: `04949ce4832856f09ea9a846b0200c7c3ed108ac1afb2173e8f95e30c9f8c866`
- 인코딩: `utf-8`

```
# 실제 종료 후 재개 재검증

**Antigravity 2.12.2에서 재검증을 통과했습니다.** 사용자 요청 한 번으로 작업 등록 → 첫 응답 종료 → 시스템 재개 메시지 → 등록 검사 실행 → 최종 종료가 이어졌습니다. 짧은 연결 시험의 결과이며 장기 작업이나 모델 전체 품질을 뜻하지 않습니다.

## 확인한 순서

| 단계 | 네이티브 기록 | 확인한 결과 |
| --- | --- | --- |
| 작업 등록 | 도구 단계 6 | 실제 새 세션 ID로 작업을 등록했습니다. |
| 첫 응답 종료 | 단계 9 | 등록만 마치고 종료 훅의 재개를 기다린다고 응답했습니다. |
| 자동 재개 | 시스템 단계 10 | 사용자가 보내지 않은 GTG 미검증 상태 메시지가 전달됐습니다. |
| 등록 검사 | 도구 단계 15 | 같은 작업의 `verify`를 실행해 회귀 검사 6개가 통과했습니다. |
| 최종 종료 | 응답 단계 17 및 저장 상태 | `verified: true`, 재개 1회, `NO_TOOL_CALL`, `fully_idle: true`, 오류 없음입니다. |

[호출 순서](sequence-evidence.json), [실제로 전달된 GTG 재개 메시지](retry-message.txt), [최종 작업 상태](final-status.json), [관측 요약](trace-summary.json)을 대조했습니다. 모델의 자기 보고만으로 판정하지 않았습니다. 직접 `hook`, `pause`, `resume`을 실행하거나 후속 사용자 요청을 보내지 않았습니다.

## 조건과 보존

- 이전 실패와 같은 프로젝트·입력·프로토콜을 사용했습니다. 새 대화의 ID는 `73a22604-7cf2-48d3-ba3e-fcfd45ba2304`이며 제목은 `종료 훅 연결 시험`입니다.
- 모델 선택 표시는 `Gemini 3.8 Flash High`, 환경은 `Local`입니다. 기존 사용자 전역 설정을 상속했으며 전역 모델·권한을 바꾸지 않았습니다.
- 입력·검사·코드·플러그인 원문이 바뀌지 않은 것을 해시로 확인했습니다. 이전의 일시 중단된 작업도 유지됐습니다.
- 첫 입력부터 마지막 응답까지 17초, 모델 응답 단계 7개, 도구 5회였습니다. 동일 조건의 반복 성능 비교가 아니므로 이전 시험보다 빨랐다는 사실을 하네스 속도 개선으로 해석하지 않습니다.
- 새로고침한 Gemini 잔여 한도는 주간 100%, 5시간 한도는 시험 전 99%·시험 후 98%였습니다. 반올림된 계정 표시이며 정확한 개별 토큰 사용량은 아닙니다. 전체 목표에서 실제 시험 요청은 누적 3회입니다.

[준비 조건](preparation.json), [실행 전 상태](state-before.sqlite3), [실행 후 상태](state-after.sqlite3), [원문·입력·상태 스냅샷](snapshot.zip)을 보존했습니다. 스냅샷은 원래 설치 위치를 포함한 보존용이며 전역 설정이나 모델 실행을 자동 재현하는 패키지가 아닙니다. 숨은 사고 내용은 복사하지 않았습니다.

검증 후 임시 플러그인은 제거 명령으로 보존 경로에 옮겼습니다. 대화와 결과는 남겼고 전역 모델·권한 설정은 바꾸지 않았습니다.

## 관측의 한계

읽기 전용 [관찰 스크립트](capture_state.py)를 시작하기 전에 실행이 끝나 [상태 기록](state-transitions.jsonl)에는 최종 상태 한 건만 있습니다. 첫 종료와 재개·검증의 순서는 네이티브 기록으로 확인했습니다. 이전 실패 시험의 원문 종료 코드를 소급해 알아낸 것은 아닙니다.

네이티브 UI 요소 오류가 반복돼 창 배치를 갱신한 뒤 제어했습니다. 같은 로컬 화면의 브라우저 접근은 클라이언트에서 차단돼 사용하지 않았습니다. 창 크기 원복은 도구로 확인하지 못했습니다.

종료 훅 재개와 같은 작업의 실제 검증 실행은 확인했습니다. 장기 작업·컨텍스트 압축 뒤 모델 행동·다른 Gemini 모델·폭넓은 자연어 요청·경쟁 도구와의 비교는 남아 있습니다.
```
