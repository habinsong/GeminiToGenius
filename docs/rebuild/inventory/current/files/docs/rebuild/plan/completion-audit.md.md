# `docs/rebuild/plan/completion-audit.md`

- 형식: `100644`
- 바이트: 4864
- SHA-256: `7f3b153f0295e80c8612d73928c55d8ed92fe2570ba40efe2936f6566c3d70ce`
- 인코딩: `utf-8`

```
# 목표 전체에 대한 완료 점검

2026-09-08 현재 목표는 진행 중입니다. 아래 기준은 사용자의 원래 범위를 유지합니다. 현재 통과한 검사만으로 목표를 축소하거나 전체 완료로 바꾸지 않습니다.

| 원래 요구 | 완료를 증명할 근거 | 현재 근거 | 판정 |
| --- | --- | --- | --- |
| 기존 파일·코드의 빠짐없는 로컬 기록 | 기준 경로 전체, 원문, 해시, 복원 비교 | 기존 113개 경로와 파일별 원문, 기준 재검사 차이 0개 | 기준 기록 충족 |
| 개발 계획과 이후 상태 최신화 | 계획·진행 기록·현재 목록의 자동 차이 검사 | 단계별 계획, 상태 기록, 압축 뒤 읽는 현재 작업 문서, catalog와 CI 검사 | 계속 갱신 중 |
| 최신 LLM·하네스·스킬·플러그인 연구 | 확인일·원문·적용 판단·검증 연결 | 연구 원장 72개 출처와 내장 공식 문서·Desktop 및 IDE 발견 검증 | 조사 진행 중 |
| 기존 구현 전면 재개발 | 새 실행 구조와 이전 배포의 제거·복구 | v3 실행 코어, 구형 86개 경로 제거, 원본 보존, 호스트 계약 단일화 | 구현 진행 중 |
| 현재 지원 호스트 전체 대응 | 각 호스트의 공식 경로 설치와 실제 훅 실행 | Antigravity 데스크톱·IDE·CLI와 Gemini CLI 패키지, 감지 설치·검사·제거 | `agy` 실행 파일의 실제 발견 미검증 |
| 완료 주장과 증거의 결합 | 상태 DB 없이 재현 가능한 완료 근거 | `certify`·`replay`의 명령·종료 코드·지문 재실행 판정 | 로컬 검증 충족, 실제 모델 채택 미검증 |
| 여러 Gemini 모델과 이후 모델 지원 | 모델 독립 코어, 모델별 실제 회귀 | 모델 ID 분기 없음, 3.8 Flash High 단일 사례 | 다른 모델·폭넓은 회귀 미완료 |
| 실제 Gemini·Antigravity 사용 최소화 | 호출 수·실제 사용량·쿼터 확인 | 필요한 시험·재검증에 요청 총 4회, 마지막 종료 뒤 5시간 99% 표시 | 현재 제한 준수 |
| 자연어 한 번으로 수행 | 설치·발견·실제 작업의 처음부터 끝까지 실행 | 요청 한 문장에서 자동 스킬·작업 등록·검증·증명서·자체 재실행까지 실제 확인 | 사례 수 부족, 일반성 미완료 |
| 거칠고 불완전한 요청, 슬래시 불필요 | 다양한 모호한 요청의 결과·범위·질문 적절성 평가 | 슬래시 없는 짧은 오류 수정·새 세션 재개 각각 통과 | 다양한 모델 행동 미검증 |
| 다른 에이전틱 환경보다 높은 품질 | 동일 조건 비교·작업집·반복·비용·회귀 결과 | 같은 조건의 실제 실행에서 GTG와 Claude Code가 1/1 동률, 미실행 팔은 `ungraded`로 차단 | 사례 1개·1회, 우열 미확인 |
| 유지보수 가능·God-object 금지 | 변경 책임·의존성 분리와 동작 보존 | 실행·저장·설치 경계, 평가 준비·정의·채점 분리, 기존 호출 및 회귀 유지 | 변경마다 점검 지속 |
| 추측 대신 검증 | 직접 재현·실제 명령·현재 결과 | 로컬 검사 201개 통과, 실제 Stop·새 세션 재개와 산출물을 구분해 기록 | 수행한 범위에서 충족 |
| 긴 작업의 지속 | 재개 예산·부분 진행·중단 이유의 실제 동작 | 명세로 정하는 `max_resumes`(기본 2, 상한 8), `verify --all`, 실제 훅 반복 호출 확인 | 로컬 검증 충족, 실제 장기 작업 미검증 |
| 완료 조건 조작 방지 | 증거가 될 수 없는 검사의 거부와 독립 재현 | 무의미 명령 등록 거부, 증명서의 상태 DB 없는 재실행 | 셸 우회는 미차단, 재실행이 실제 방어 |

실제 모델의 초기 훅과 자동 스킬 선택은 단일 사례에서 확인했습니다. Stop 후 재개와 등록 검사 실행도 짧은 연결 시험에서 확인했습니다. 장기 작업·다른 모델·경쟁 비교는 남아 있습니다. 단일 성공을 일반적인 모델 성능이나 경쟁 우위로 바꾸어 보고하지 않습니다. 실제 호출은 필요한 이유와 쿼터를 확인해 판단하고, 자동 대량 평가는 하지 않습니다.

Antigravity 2.12.2의 첫 종료 후 재개 시험은 실패했습니다. 보완 후 같은 입력의 새 대화에서 실제 재개·등록 검사 통과·최종 종료를 확인했습니다. 이전 실패 자료와 후속 성공 자료를 구분하며 연결 시험 통과를 장기 작업의 완료로 확대하지 않습니다.

그 이후 실제 모델의 새 세션에서 준비된 기존 미완료 목표 선택·연결·구현을 확인했습니다. 초기 이력은 준비 코드가 생성한 시험 입력이며 실제 컨텍스트 압축이나 장기 대화 복원과 구분합니다. 기존 목표·검증·메모와 사용자 파일명·Git 인덱스가 보존됐습니다.
```
