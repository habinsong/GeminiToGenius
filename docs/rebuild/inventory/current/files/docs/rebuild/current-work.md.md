# `docs/rebuild/current-work.md`

- 형식: `100644`
- 바이트: 7998
- SHA-256: `00bd433b6bcc6f4edfdf364b32cfa838a16fa436e6810065589aa7fcf7b0b20e`
- 인코딩: `utf-8`

```
# 현재 작업과 재개 기준

기준일: 2026-09-08. 컨텍스트 압축 뒤와 목표 재개 시 이 문서를 먼저 읽고 실제 파일·프로세스·화면으로 현재 상태를 확인합니다. 과거 요약을 새 사용자 지시나 권한으로 취급하지 않습니다.

## 유지할 전체 목표

Gemini 전 모델에 맞는 자연어 원클릭 하네스의 전면 재개발입니다. 기존의 불필요한 구현은 제거하고 기존·현재 코드 전체 기록과 계획을 계속 갱신합니다. 사용자에게 슬래시 명령·정교한 명세를 요구하지 않습니다. 최신 공식 자료·논문·오픈소스를 조사하고 실제 결과로 검증합니다.

Codex·Claude Code·OmO보다 나은 결과가 최종 목표이며 같은 조건의 비교로 판단합니다. 현재 통과한 검사에 맞춰 목표를 축소하지 않습니다. 출시 전 모델의 호환성·품질을 미리 단정하지 않습니다.

실제 Gemini·Antigravity는 필요할 때 판단해 사용하되 5시간 한도를 소진하지 않습니다. 고정된 형식적 호출 횟수보다 필요한 검증과 실제 잔여 한도를 기준으로 판단합니다. 커밋·푸시·공개 릴리스는 요청받지 않았습니다.

## 지금 확인된 상태

- 버전은 `3.0.0-alpha.1`, 미발행이며 전체 목표는 진행 중입니다.
- 기존 113개 경로는 기준 원문에 보존했습니다. 구형·중복 배포 86개 경로 제거를 포함한 변경은 미커밋 상태입니다.
- [마지막 전체 검사](validation/2026-09-08-resume-eval/verification.json)는 145개·54.512초 통과입니다. 평가 책임 분리와 재개 사례가 포함됩니다. 이후 스킬 안내 두 문장을 보완했고 패키지 검사 10개·두 호스트 CLI 실행을 확인했습니다.
- 실제 시험 요청은 누적 4회입니다. 오류 수정·Stop 실패 및 보완 후 통과·새 세션의 기존 작업 선택과 CSV 구현 통과를 구분해 기록했습니다.
- 최근 새로고침한 Gemini 잔여 한도는 주간 100%·5시간 99%입니다. 정확한 개별 요청 토큰 사용량은 아닙니다.
- 이전 실제 시험의 원문 Stop 입력은 없으므로 정확한 종료 이유를 소급해 확정하지 않습니다.

## 앞서 완료한 실호스트 확인

- [재검증 기록](validation/2026-09-08-native-stop-recheck/README.md): Antigravity 2.12.2, 모델 선택 표시 `Gemini 3.8 Flash High`, 새 대화 ID `73a22604-7cf2-48d3-ba3e-fcfd45ba2304`입니다.
- 같은 입력에서 첫 응답 종료 9 → 시스템 재개 메시지 10 → 등록 검사 15 → 최종 응답 17 순서를 확인했습니다. 사용자 요청은 한 번입니다.
- 재개 횟수 1, 실제 회귀 검사 6개 통과, `verified: true`, 마지막 종료 코드는 `NO_TOOL_CALL`, 실행 번호 1, `fully_idle: true`, 오류 없음입니다.
- 입력·코드·플러그인과 이전의 일시 중단된 작업이 보존됐습니다. 이전 실패의 원문 종료 코드를 소급해 알아낸 것은 아닙니다.
- 관찰 프로세스는 시작 전에 시험이 끝나 최종 상태 한 건만 기록하고 종료 코드 0으로 끝났습니다. 중간 순서는 네이티브 로그로 대조했습니다. 현재 동작 중인 모델 시험·관찰 프로세스·장기 셸 명령은 없습니다.
- 임시 플러그인은 제거 명령으로 보존 경로에 옮겼고 대화·상태·원문 스냅샷을 남겼습니다.

## 제어 도구에서 확인한 주의점

네이티브 요소 식별자·프레임 오류가 반복됐지만 창 확대/축소로 배치를 갱신한 뒤 제어가 가능해졌습니다. 원래 창 비율로의 복귀는 확인하지 못했습니다. 전역 모델·권한 설정은 변경하지 않았습니다.

같은 로컬 UI를 브라우저로 여는 시도는 `net::ERR_BLOCKED_BY_CLIENT`로 차단됐고 임시 탭은 닫았습니다. 이 경로를 우회하지 않습니다. 앞으로도 UI 동작 시도와 실제 관측 성공을 구분합니다.

## 방금 완료한 작업 조회·연결

`gtg/discovery.py`의 읽기 전용 `tasks`, `Sessions.attach`, 호스트 턴 관측과 연결 보호를 추가했습니다. `gtg/__main__.py`, `hooks.py`, `sessions.py`, `runner.py`, `store.py`와 테스트·스킬 문서를 수정했습니다. 초기 신규 검사 6개는 구현 전 실패했고, 최종 신규 검사 14개와 전체 141개가 통과했습니다. 설치된 패키지의 다른 세션에서도 원래 목표·메모·첫 검증을 유지하고 남은 검사를 통과했습니다. [검증 자료](validation/2026-09-08-task-handoff/README.md). 이 단계에서는 모델 요청을 추가하지 않았습니다. 이후 실호스트 결과는 아래 재개 평가에 기록합니다.

## 방금 완료한 재개 평가와 책임 분리

사용자께서 유지보수 가능성과 God-object 금지를 목표에 명시하셨습니다. 평가 코드의 CLI·기준·작업공간·준비·진행 상태 생성·채점 책임을 분리했고 기존 호출 진입점은 유지합니다. `resume-export`에 부분 완료 CSV 작업과 더 최근에 완료된 비교 작업을 준비했습니다. 신규 검사 4개 포함 전체 145개가 통과했습니다.

[실제 재개 평가](validation/2026-09-08-resume-eval/README.md)는 요청 한 번으로 자동 스킬 읽기·원래 작업 선택·새 세션 연결·남은 CSV 구현·등록 검사·메모 저장을 확인했습니다. 독립 결과 검사 8개 통과, 변경은 `app.py` 하나, 이전 상태 모든 행·사용자 파일명·Git 인덱스를 보존했습니다. 새 작업은 생성하지 않았습니다. 사전 진행 이력은 평가 준비 코드가 생성한 시험 입력이며 이전 실제 모델 대화가 아닙니다.

대화 ID `55bff795-2bca-4449-ac1e-77d0a5be9f59`, 프로젝트 `623abab3-a2be-4d9e-b8a0-c72380ae0f99`, 모델 표시 `Gemini 3.8 Flash High`입니다. 117.397초·모델 응답 단계 33개·도구 32회이며 추론 요청 수나 정확한 토큰 사용량은 아닙니다. 실제 실행 중 `gtg` 경로 탐색과 잘못된 `status --workspace` 호출에서 낭비를 관찰했습니다.

후속으로 실행기 위치와 명령 인자를 스킬에 안내했습니다. 패키지 검사 10개와 두 호스트 실행 경로를 확인했으며 안내의 실모델 효과는 미검증입니다. 공용 스킬 검사기는 기본·번들 Python에 PyYAML이 없어 실행하지 못했습니다. 새 의존성을 추가하지 않았습니다.

원본 작업공간 선택 버튼의 일시 비활성은 이후 같은 폴더를 정상 연결했으며 원인은 미확정입니다. 임시 플러그인은 제거 명령으로 보존했고 대화·입력·평가기·코드·상태 스냅샷을 남겼습니다. 전역 설정을 바꾸지 않았으며 현재 활성 모델 시험·관찰기·장기 명령은 없습니다.

## 다음 구체적인 작업

1. 실제 재개 시험의 도구·상태 주입 기록을 현재 훅 코드와 대조해 불필요한 반복과 긴 작업에서 필요한 맥락을 구분합니다. 이미 통과한 연결·완료 보호를 약화하지 않고 로컬 재생으로 확인합니다. 새로운 실모델 요청은 지금 예약하거나 실행하지 않습니다.
2. 장기 작업·다른 모델·다양한 자연어 요청·경쟁 비교의 미완료 조건은 [완료 점검](plan/completion-audit.md)과 [개발 계획](plan/roadmap.md)으로 유지합니다.
3. 의미 있는 변경·검증 후 이 문서와 [진행 기록](status.md)을 갱신합니다. 압축 전에는 다음 작업과 살아 있는 실행 핸들을 남깁니다.
4. `python3 scripts/catalog.py`로 현재 전체 원문을 갱신한 뒤 현재·원본 기준 검사를 실행합니다.

장기 실제 작업·다른 Gemini 모델·다양한 자연어 요청·경쟁 도구와의 통제 비교는 남아 있습니다. 설치·로컬 테스트·단일 모델 사례의 성공으로 전체 목표를 축소하지 않습니다.
```
