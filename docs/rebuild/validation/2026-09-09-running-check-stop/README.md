# 실행 중인 검사가 완료 보고를 막지 못하던 구멍

확인일: 2026-09-09. 실제 모델 요청은 0회입니다. 실제 훅 프로세스에 공식 계약대로 만든 입력을 넣어 확인했습니다.

## 어떻게 발견했나

훅은 사용자의 실제 세션에서 매 턴 실행되므로, 나쁜 조건을 실제로 만들어 응답을 확인했습니다. 한 작업에 대해 Stop 훅을 세 시점에 호출했습니다.

```
[검사 실행 전(미검증)] 75ms  {"decision": "continue", "reason": "GTG 미완료 상태(데이터): ..."}
[검사 실행 중]        83ms  {"decision": "stop"}
[검사 통과 뒤]        75ms  {"decision": "stop"}
```

가운데가 틀렸습니다. 검사가 **실행 중**이면 아무것도 검증되지 않았는데 훅이 종료를 조용히 허용했습니다. 이 하네스가 존재하는 이유인 "미검증을 완료로 보고하지 못하게 한다"가 그 구간에서 통하지 않았습니다.

처음 만든 입력은 필드 이름이 틀려(`workspaceRoots`·`sessionId`) 훅이 입력 검증에서 되돌아갔고, 그 상태의 응답을 정상 동작으로 오해할 뻔했습니다. 공식 계약의 `workspacePaths`·`conversationId`로 고쳐 다시 측정한 값이 위 결과입니다. 같은 이유로 이전에 기록한 "훅 왕복 20ms"는 인자 오류 경로의 값이었습니다. 정상 경로는 75~90ms입니다.

## 원인

`gtg/hooks.py`의 `resume()` 첫 줄이 두 개의 다른 결정을 한 값으로 합쳤습니다.

```python
if any(check["status"] == "running" ...):
    return empty      # empty == {"decision": "stop"}
```

앞의 판단("실행 중인 검사를 재개로 중복 실행하지 않는다")은 옳습니다. 재개는 사용자 쿼터를 쓰고 이미 도는 검사를 겹치게 합니다. 그러나 그 결론을 `empty`, 즉 **종료 허용**으로 돌려주면서 뒤의 판단("미검증을 완료로 통과시키지 않는다")까지 함께 버렸습니다.

이 상태는 드물지 않습니다. 검사를 배경으로 돌린 경우, 사용자가 터미널에서 따로 검증을 돌리는 경우, 그리고 **중단된 실행이 `finished IS NULL` 기록을 남긴 경우**가 모두 여기에 걸립니다. 마지막 경우는 `recover` 전까지 영구히 지속되므로, 그 작업의 모든 완료 보고가 계속 조용히 허용됩니다.

기존 검사 308개 중 이 경로를 덮는 것이 하나도 없었습니다.

## 고침

`still_running`으로 두 결정을 분리했습니다. 재개는 그대로 하지 않고(예산 소비 0), 종료에는 이유를 붙입니다.

```
[검사 실행 중] 90ms {"decision": "stop", "reason": "GTG: 등록된 검사가 아직 실행 중이라 완료가
검증되지 않았습니다(slow). 자동 재개는 하지 않습니다. 검사 결과를 확인하고, 중단된 실행이면
recover 뒤 다시 검증하세요."}
```

회귀 검사 두 개를 추가했고, 고치기 전 동작으로 되돌려 둘 다 실패하는 것을 확인했습니다.

## 전달 경로의 실제 근거

고친 뒤에 두 호스트의 출력 계약을 다시 조사했고, 결과가 서로 달랐습니다.

**Gemini CLI(`AfterAgent`): 확인됨.** `systemMessage`는 `decision`·`continue`를 보지 않는 경로에서 무조건 발행됩니다(`packages/core/src/hooks/hookEventHandler.ts`의 `logHookExecution`). 즉 종료를 막지 않고 사용자 화면에 표시할 수 있습니다. 공식 문서도 "`suppressOutput`은 배경 로깅에만 영향을 주며 `systemMessage`는 여전히 사용자에게 표시된다"고 적습니다. 그래서 이 호스트에는 `{"systemMessage": ...}`를 내보내도록 구현했습니다. 다만 **대화형 터미널에서만** 표시되고(비대화형 `-p` 모드에서는 구독자가 없어 사라집니다) **모델 컨텍스트에는 들어가지 않습니다.** 모델에 닿게 하려면 `decision: "deny"`로 재시도를 강제해야 하는데, 이 상황에는 맞지 않습니다.

**Antigravity(`Stop`): 문서에 없음.** 공식 훅 문서의 Stop 출력 필드는 `decision`과 `reason` 둘뿐이며, `reason`은 **`decision`이 `"continue"`일 때만** "시스템 메시지로 대화에 주입된다"고 규정합니다. `"stop"`이라는 값 자체가 문서에 없고("그 밖의 값은 종료를 허용한다"), 종료를 허용하는 응답의 `reason`이 어디로 가는지는 어떤 공식 문서에도 없습니다. 즉 **이 메시지는 전달되지 않을 수 있습니다.**

그래도 이 값을 유지합니다. 무시되면 종전과 같은 조용한 허용이고, 전달되면 사실이 드러납니다. 무엇보다 이번 수정의 핵심은 메시지가 아니라 **미검증을 통과시키던 자리에서 사실을 만들어 내보내는 것**이며, 같은 미검증 상태는 다음 턴의 `PreInvocation`에서 다시 안내됩니다. 이미 쓰던 재개 상한 도달 응답(`exhausted`)도 같은 형식이므로 이번에 위험이 새로 생기지는 않습니다.

문서화된 대안은 `PostInvocation`의 `injectSteps`와 `terminationBehavior: ""`입니다. 다만 `PostInvocation`의 실행 시점은 제품별 문서가 엇갈려 이 저장소가 이미 "동작을 가정해 종료 루프를 만들지 않는다"고 정해 둔 부분이라, 실호스트 확인 전에는 채택하지 않습니다.

## 남은 한계

- Antigravity에서 종료를 허용하며 붙인 `reason`이 실제로 표시되는지 확인하지 못했습니다. 실호스트 시험에서 확인할 항목입니다.
- Gemini CLI의 `systemMessage`는 비대화형 실행에서 사라지고 모델에는 닿지 않습니다.

## 함께 본 것

같은 실험에서 훅의 다른 나쁜 조건도 확인했습니다.

| 조건 | 결과 |
| --- | --- |
| GTG를 한 번도 쓰지 않은 폴더 | 종료 0, `{}`, 폴더에 아무 파일도 남기지 않음 |
| 쓰기 금지 작업공간 | 종료 0, `{}` |
| 상태 파일이 깨진 경우 | 종료 0, 종료 허용(고장 시 열림) |

[중단 복구 기록](../2026-09-09-reboot-recovery/README.md)과 함께 보면, 중단된 실행이 남긴 기록은 이제 복구할 수 있고(잠금 판단) 그 전까지 완료로 통과하지도 않습니다(이 기록).
